{
  "$schema": "https://openapi.vercel.sh/2.0/schema.json",
  "buildCommand": "uv pip install --system -r requirements.txt",
  "devCommand": "uv run uvicorn api.main:app --host 0.0.0.0 --port 8000",
  "installCommand": "uv pip install --system -r requirements.txt",
  "framework": "fastapi"
}
